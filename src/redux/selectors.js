export const carsSelector = state => state.cars.cars;
export const favoritesSelector = state => state.favorites.favList;
export const makesSelector = state => state.filters.favList;
export const currentPageSelector = state => state.cars.currentPage;
